<?php
// Пример проблемного PHP кода для тестирования PHP синтаксического анализатора
// Ожидаемые проблемы: синтаксические ошибки, отсутствующие точки с запятой

// Отсутствующая точка с запятой (Parse error)
$name = "John"
$age = 25

// Неправильные скобки (Parse error)
function calculate($a, $b {
    return $a + $b;
}

// Неправильное использование переменных (Warning)
echo $undefined_variable;
